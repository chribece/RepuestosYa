version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: repuestosya-redis
    ports:
      - "6379:6379"
    restart: unless-stopped
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes

volumes:
  redis_data:
